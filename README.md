# ROJO-MOJO

it is a encryptor that encrypts your message with your own custom encryption key if you have one or it will generate its own 256- sha sum and encrypt it. It will decrypt your message on the basis of encryption text and encryption key
